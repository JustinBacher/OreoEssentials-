enabled: true

# ─────────────────────────────────────────────────────────────────
# Group RTP Portals
# Each key under 'portals:' is the portal's unique ID (lowercase).
# Create portals in-game with /grouprtp create <id>
# then edit this file and /grouprtp reload for fine-tuning.
# ─────────────────────────────────────────────────────────────────
portals:

  wilderness_gate:
    # Display name (supports & colour codes)
    name: "&6&lWilderness Portal"

    # World the portal region is in
    world: "spawn"

    # Bounding box corners (set in-game with /grouprtp wand)
    region:
      pos1: [100.0, 60.0, 100.0]
      pos2: [105.0, 68.0, 105.0]

    # ── RTP settings ────────────────────────────────────────────
    rtp:
      # World to teleport players into
      world: "survival"

      # Leave empty for same server; set to BungeeCord server name for cross-server
      server: ""

      # Search radius from center (blocks)
      radius: 5000.0
      min-radius: 500.0

      # Center of the RTP area (default: world origin 0,0)
      center-x: 0.0
      center-z: 0.0

      # Y bounds for the ground search
      min-y: 50
      max-y: 250

      # How many random locations to try before giving up
      attempts: 50

      # Blocks that are never safe to land on
      unsafe-blocks:
        - LAVA
        - FIRE
        - CACTUS
        - MAGMA_BLOCK
        - POWDER_SNOW
        - WITHER_ROSE
        - SWEET_BERRY_BUSH
        - CAMPFIRE
        - SOUL_CAMPFIRE

      # Biome names to avoid (leave empty to allow all)
      blacklisted-biomes: []

    # ── Group behaviour ──────────────────────────────────────────
    group:
      # Minimum players needed to start the countdown
      min-players: 2

      # Maximum players allowed in one session
      max-players: 10

      # Countdown duration in seconds
      countdown: 10

      # Players scatter within this many blocks of the base landing spot
      cluster-radius: 5.0

    # Cooldown per player in milliseconds (300000 = 5 minutes)
    cooldown: 300000

    # Permission node required to use this portal (empty = no requirement)
    permission: ""

    # ── Messages ────────────────────────────────────────────────
    messages:
      join:        "&a&l+1 &7Player entered! &e{current}&7/&e{max} &7| Need &e{min}&7 to launch"
      leave:       "&c&l-1 &7Player left. &e{current}&7/&e{min} &7needed"
      countdown:   "&6Teleporting in &e{seconds}s &7\u2014 &e{current} &7players ready"
      cancelled:   "&cTeleportation cancelled \u2014 not enough players"
      searching:   "&e&lFinding safe wilderness location..."
      success:     "&aYou have been teleported to the wilderness!"
      full:        "&cPortal full (&e{max} &cmax). Try again later"
      cooldown:    "&cWait &e{time} &cbefore using this portal again"
      no-location: "&cCould not find a safe location. Please try again"
      no-permission: "&cYou don't have permission to use this portal"

    # ── Effects ─────────────────────────────────────────────────
    effects:
      ambient-particle:  "PORTAL"
      ambient-count:     3
      teleport-particle: "END_ROD"
      teleport-count:    30
      sound-enter:       "BLOCK_PORTAL_AMBIENT"
      sound-teleport:    "ENTITY_ENDERMAN_TELEPORT"
